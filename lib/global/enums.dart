// Planner Tools
enum PTools { none, pin, shape, connector }

// Planner Shapes
enum PShapes { none, square, triangle, circle, rectangle, poly }

// History Actions
enum HAction { add, delete, modify }

// History Categories
enum HCategory { symbol, shape, polyline, fill }
